export const enum ReviewStatus {
  DEFAULT = "default",
  HOME = "home",
  ARCHIVED = "archived",
}

export const enum OrderStatus {
  ACCEPTED = "accepted",
  REJECTED = "rejected",
  PENDING = "pending",
  COMPLETED="completed",
  INPOMPLETED = 'incompleted',
  INPOMPLETE = 'incomplete',
  SUCCEEDED = 'succeeded',
}
